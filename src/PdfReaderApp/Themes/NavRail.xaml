<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Mục điều hướng nav rail (M3): icon trong "active indicator" pill + nhãn dưới.
         Tag = chuỗi nhãn (Content đã dành cho icon). Màu lấy từ token #58 qua DynamicResource. -->
    <Style x:Key="TriThu.NavRailItem" TargetType="ToggleButton">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="72"/>
        <Setter Property="Margin" Value="0,4"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <StackPanel Background="Transparent">
                        <Border x:Name="Indicator" Width="56" Height="32" CornerRadius="16"
                                Background="Transparent" HorizontalAlignment="Center">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{Binding Foreground,
                                                  RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Border>
                        <TextBlock Text="{TemplateBinding Tag}" HorizontalAlignment="Center" Margin="0,4,0,0"
                                   FontFamily="{DynamicResource TriThu.Font.Sans}"
                                   FontSize="{DynamicResource TriThu.Font.Size.Label}"
                                   Foreground="{TemplateBinding Foreground}"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Indicator" Property="Background"
                                    Value="{DynamicResource TriThu.Brush.SecondaryContainer}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Indicator" Property="Background"
                                    Value="{DynamicResource TriThu.Brush.PrimaryContainer}"/>
                            <Setter TargetName="Indicator" Property="TextElement.Foreground"
                                    Value="{DynamicResource TriThu.Brush.PrimaryContainer.On}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Mục hành động nav rail (AI / Chú thích / Cài đặt): cùng layout icon+nhãn, chỉ hover, không active. -->
    <Style x:Key="TriThu.NavRailAction" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="72"/>
        <Setter Property="Margin" Value="0,4"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <StackPanel Background="Transparent">
                        <Border x:Name="Hover" Width="56" Height="32" CornerRadius="16"
                                Background="Transparent" HorizontalAlignment="Center">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{Binding Foreground,
                                                  RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Border>
                        <TextBlock Text="{TemplateBinding Tag}" HorizontalAlignment="Center" Margin="0,4,0,0"
                                   FontFamily="{DynamicResource TriThu.Font.Sans}"
                                   FontSize="{DynamicResource TriThu.Font.Size.Label}"
                                   Foreground="{TemplateBinding Foreground}"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Hover" Property="Background"
                                    Value="{DynamicResource TriThu.Brush.SecondaryContainer}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
